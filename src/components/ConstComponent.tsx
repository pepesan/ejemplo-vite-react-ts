// const SimpleComponent = ({ message }) => {
const ConstComponent = () => {
    // que presenta un div con el contenido en texto de message
    return <div>ConstComponente</div>;
};

export default ConstComponent;
